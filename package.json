{
  "name": "bookstore-app",
  "version": "0.1.0",
  "private": true,
  "description": "Monorepo root: /client (Vite+React SPA) + /server (Express API). See README.md for dev setup.",
  "scripts": {
    "seed:mysql": "node scripts/seed-mysql.mjs",
    "seed": "node scripts/seed.mjs",
    "create-admin": "node scripts/create-admin.mjs"
  },
  "devDependencies": {
    "mysql2": "^3.22.5",
    "playwright": "^1.61.1"
  }
}
